<form action="{{ action }}" method="post">
  <input type="hidden" name="operation_xml" value="{{ xml }}">
  <input type="hidden" name="signature" value="{{ signature }}">
  <div class="buttons">
    <div class="pull-right">
      <button type="submit" class="primary">{{ button_confirm }}</button>
    </div>
  </div>
</form>
