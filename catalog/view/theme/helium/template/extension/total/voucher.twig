<details aria-labelledby="enter_voucher">
  <summary><h4 id="enter_voucher">{{ heading_title }}</h4></summary>
    <div class="form-group">
      <label for="input-voucher">{{ your_voucher }}</label>
      <div class="input-group">
        <input type="text" name="voucher" value="{{ voucher }}" placeholder="{{ entry_voucher }}" id="input-voucher" />
        <button type="submit" id="button-voucher" class="primary">{{ ok }}</button>
      </div>
      <span>{{ entry_voucher }}</span>
    </div>
  </div>
</details>

      <script type="text/javascript">
	  
		// fetch('index.php?route=extension/total/voucher/voucher',{method: "POST"})
		// .then((r) => {return r.text();})
		// .then((body) => {
		// 	let step = document.querySelector('#collapse-voucher')
		// 	step.innerHTML = body;
		// });
		
	 
	  <!--
// $('#button-voucher').on('click', function() {
// 	$.ajax({
// 		url: 'index.php?route=extension/total/voucher/voucher',
// 		type: 'post',
// 		data: 'voucher=' + encodeURIComponent($('input[name=\'voucher\']').val()),
// 		dataType: 'json',
// 		beforeSend: function() {
// 			$('#button-voucher').button('loading');
// 		},
// 		complete: function() {
// 			$('#button-voucher').button('reset');
// 		},
// 		success: function(json) {
// 			$('.alert-dismissible').remove();
			
// 			if (json['error']) {
// 				$('.breadcrumb').after('<div role="alert" class="alert-danger"><i class="icon-alert"></i> ' + json['error'] + '<button type="button" class="close"></button></div>');
				
// 				$('html, body').animate({ scrollTop: 0 }, 'slow');
// 			}
			
// 			if (json['redirect']) {
// 				location = json['redirect'];
// 			}
// 		},
// 		error: function(xhr, ajaxOptions, thrownError) {
// 			alert(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
// 		}
// 	});
// });
//--></script>

