{{ header }}
{% include 'helium/template/common/breadcrumbs.twig' %}
<main id="common-success">
  <div class="row">
    {{ content_top }}
    <div>
      <h1>{{ heading_title }}</h1>
      {{ text_message }}
      <div class="row">
        <a href="{{ continue }}" class="primary">{{ button_continue }}</a>
      </div>
    </div>
  </div>
</main>
{{ column_left }}
{{ column_right }}
{{ content_bottom }}
{{ footer }}